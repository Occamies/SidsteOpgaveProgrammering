
export default class RotationOnY {
  constructor(world){

    this.world = world

    //*camera*/

    this.world
    //this.world.camera.rotation.set(0,Math.PI*1.5,0) /* base rotation */
    //world.camera.rotation.set(0,Math.PI*1.2,0) /* min rotation */
    //world.camera.rotation.set(0,Math.PI*1.8,0) /* max rotation */
    //*END camera*/
  }
}